tags
test.sh
.luarc.json
nvim

spell/

# nvim-pack-lock.json is tracked in git so fresh clones install all plugins
# at exactly the same revisions (vim.pack reads it on first startup)

.DS_Store

# lua-language-server workspace cache
.cache/
